const reducer = (state, action) => {
    switch(action.type){
        case 'DELETE_TASK':
            // return{
            //     list: list.filter(el => el.id !== action.payload)
            // }
    }
}

export default reducer